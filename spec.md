# Tank Battle — 專案規格書 (spec.md)

> 本文件基於對 `index.html`（~900 行，遊戲本體）與 `music.html`（音樂原型測試頁）的實際程式碼閱讀，加上與專案負責人（以下稱「你」）的訪談整理而成。此前本專案完全透過臨時的對話式需求逐步建成，沒有任何規格文件；本文件是第一份正式 spec，之後的變更應以此為基準持續更新。

## 1. Overview & Goals

- **專案類型**：Three.js 瀏覽器 3D 坦克對戰小遊戲。原本是單一 HTML 檔案、無建置流程、可直接雙擊以 `file://` 開啟；**此約束已於後續討論中解除**（見 §4），主要部署管道改為 GitHub Pages（或同等的靜態 HTTPS 託管），開發時可接受起本地伺服器測試。
- **專案定位**：這不是用完即丟的一次性 demo，而是**打算持續開發的基礎（base for continued development）**。因此本 spec 會比「隨手做做」的專案更講究一點嚴謹度——之後每次疊代都建議回頭更新這份文件。
- **目前狀態**：核心玩法（移動、瞄準、射擊、敵人 AI、道具、音效、背景音樂）與無限生存模式（波次/技能點/方位裝甲/武器與飛彈升級）已經可以運作，玩法可調參數已集中到 `src/config.js`。新增偵查兵/重裝兵敵人種類、10 關關卡資料（含沙塵地形效果）與測試模式（QA 用沙盒，可自由選敵人/關卡/數值，見 §6.5），闖關模式本身的過關流程仍待實作（見 §6.3）。程式碼中留有三個尚未回復的「測試用旗標」（見 §3）。

## 2. Current Feature Set（現狀，以程式碼為準）

### 2.1 檔案結構

專案已依 §4 的決定拆成標準 ES module，`index.html` 現在是純 HTML 外殼（~64 行：CSS、HUD 標記、importmap、一行 `<script type="module" src="./src/game.js">`），實際邏輯在 `src/` 底下：

| 檔案 | 內容 |
|---|---|
| `src/game.js` | 場景/相機/渲染器、地圖與圍牆、玩家、敵人與 AI、道具、飛彈、砲彈、粒子、輸入處理、碰撞、HUD、主迴圈（進入點，由 HTML 直接載入） |
| `src/tank.js` | `makeTank(color)` 坦克模型工廠函式（玩家與敵人共用） |
| `src/audio.js` | 所有 Tone.js 音效合成器與 `play*()` 函式、三首背景音樂的播放清單系統、`ensureAudio()` / `setMuted()` |
| `src/marseillaise-midi.js` | 純資料檔，只放馬賽曲 MIDI 的 base64 字串（供 `audio.js` 匯入） |
| `src/menu.js` | 開場模式選單（`showModeMenu()`），選好模式後回呼 `game.js` |
| `src/survival.js` | 無限生存模式的波次/技能點/方位裝甲/武器/飛彈升級數值與配點 UI（見 §6.2） |
| `src/config.js` | 集中管理的玩法可調參數 `CONFIG` 物件（見 §5.2），含 `enemyTypes`（偵查兵/重裝兵數值）與 `terrain.dustSlowMult`（沙塵區減速倍率） |
| `src/levels.js` | 闖關模式/測試模式共用的 10 關關卡資料（`LEVELS`，見 §6.3），每關 `{ name, terrain, walls, dust? }` |
| `src/testmode.js` | 測試模式設定面板（`showTestMenu()`，見 §6.5）：選敵人種類、選關卡、直接套用裝甲/武器/飛彈數值 |

**本地開發**：因為改用真正的 `import`/`export`，`file://` 雙擊已不再可行（見 §4），本地測試需要起一個靜態伺服器，例如在 `tank_battle/` 目錄下跑 `python3 -m http.server`，再開 `http://localhost:8000/index.html`。

拆分時做過完整的內容比對（重組後跟拆分前的原始腳本逐行 diff），確認除了預期的改動（`setMuted()` 抽出、MIDI base64 移到獨立檔案）以外，沒有任何邏輯遺漏或跑掉。

### 2.2 核心玩法（`src/game.js` 等）
- **場景**：Three.js 場景，`ARENA = 36`（正方形半邊長 36），外圍牆 + 內部掩體（`layout` 陣列定義的矩形障礙物），有霧效（`arenaFog`，55–95 距離淡出）。
- **操作**：WASD 移動（A/D 轉向、W/S 前進後退，履帶車式移動，不能側移）、滑鼠瞄準砲塔（獨立於車身朝向）、滑鼠左鍵或空白鍵開砲。操作提示文字（`#help`）位於畫面右上角喇叭圖示（`#muteBtn`）下方，深色底加陰影提高可讀性。
- **碰撞**：圓形（坦克/子彈）對**可旋轉矩形**（牆）碰撞偵測，`collideWalls()` / `bulletHitsWall()`。`addWall(x, z, w, d, h, rot)` 第 6 個參數可指定旋轉角度（弧度），內部用「先把世界座標轉回牆的局部座標系再做矩形判定，算完再轉回世界座標」處理（跟砲塔瞄準、方位裝甲用的是同一套 Y 軸旋轉數學）；`layout` 陣列每筆資料第 5 個欄位可選填旋轉角度，不填預設 0（向下相容）。牆的 `min`/`max`（旋轉後的外接框）仍保留給道具生成點的粗略避開判定用。
- **玩家坦克**：`player` 物件，`hp: 100`、`speed: 9`、`radius: 1.7`，冷卻 0.35s（火力強化時 0.12s），子彈傷害 10（強化時 20）。
- **敵人坦克**：由同一個 `makeTank(color)` 工廠函式產生（僅顏色不同：玩家綠色 `0x7fbf5f`，敵人紅棕色 `0xc75c4a`），造型為 T-34 風格（傾斜首上裝甲 glacis、車尾引擎艙+排氣管、六角砲塔前移）。敵人 `hp: 30`、`speed: 4.5`，子彈傷害 12。
- **敵人 AI**：
  - 砲塔獨立瞄準玩家（不受車身朝向牽制），可以邊轉邊打。
  - 車身依距離切換三種行為：`dist > ENGAGE_DIST(16)` 時逼近、`dist < RETREAT_DIST(8)` 時掉頭拉開、中間距離則沿切線方向繞圈/側移（`strafeDir`）。
  - 過去曾有「車身瞄準公式 180 度反向」的 bug，目前已修正。
  - 敵人上限同時存在 3 隻，`spawnTimer = 4` 秒間隔生成，出生點為地圖四角（`SPAWNS`）。
- **音效**（Tone.js 合成，無音檔）：開砲、撞牆/彈道命中牆、擊中、爆炸、遊戲結束音效、道具拾取音效、飛彈齊射警報音，皆為程式合成（`MembraneSynth`、`NoiseSynth`、`MetalSynth` 等）。
- **背景音樂**：三首曲目的輪播架構（`PLAYLIST`，`playNextTrack()` 依 `duration` 用 `setTimeout` 串接下一首）：
  1. 史詩感 D 小調進行（`EPIC_PROGRESSION` + `EPIC_THEME`，手寫音符）。
  2. 明亮愉快 C 大調 oom-pah 節奏（`BRIGHT_PROGRESSION` + `BRIGHT_MELODY`，手寫音符）。
  3. 柴可夫斯基《1812 序曲》中「馬賽曲」主題的真實演奏，來源是實際 MIDI 檔案內嵌成 base64（`MARSEILLAISE_MIDI_BASE64`），執行期用 `@tonejs/midi` 解析成音符（`marseillaiseMidi`），而非手刻音符。
- **道具系統**：地圖上會生成三種拾取物：
  - `shield`（護盾）：8 秒無敵。
  - `rapid`（火力強化）：8 秒射速加快、傷害提升。
  - `missile`（自動鎖定飛彈齊射）：對場上所有敵人各發射一枚追蹤飛彈，飛行期間鏡頭平滑拉遠至全地圖俯瞰視角，結束後平滑回到跟隨鏡頭（`MISSILE_CAM_DURATION = 5.5` 秒）。
  - 正常權重是 `{ shield: 4, rapid: 4, missile: 2 }`（見 §3）。

### 2.3 `music.html`
- 獨立的音樂原型測試頁，用來在合併進遊戲前先驗證「MIDI 內嵌 base64 + @tonejs/midi 解析播放馬賽曲」這個技術方案是否可行。
- 與遊戲本體無程式碼共用關係，純粹是開發過程中的沙盒/暫存頁面，**目前沒有計畫要正式收編進專案**（見 §7 開放問題）。

## 3. Known Testing Flags / Temporary State

程式碼中目前有三個「測試階段」旗標，皆為開發過程中留下、尚未回復成正常行為：

| 旗標 | 位置 | 目前效果 | 正常行為（旗標移除後） |
|---|---|---|---|
| `TESTING_ONLY_TRACK = 2` | `src/audio.js` | 開場只會固定播放第 3 首（馬賽曲/1812序曲），`playNextTrack()` 輪播邏輯程式碼仍在但被繞過，永遠不會輪到另外兩首 | 呼叫 `playNextTrack()`，三首曲目依序輪播 |
| `TESTING_ONLY_POWERUP = 'missile'` | `src/game.js` | 所有道具生成一律變成飛彈齊射道具，且重生間隔被縮短為 3–6 秒（方便測試），跳過正常的加權隨機選擇 | 依 `POWERUP_KINDS` 權重（shield:4, rapid:4, missile:2）加權隨機生成，重生間隔 10–18 秒 |
| `TESTING_FAST_WAVES = true` | `src/survival.js` | 無限生存模式每波只需擊殺 2 隻（正式為 8 隻），且每波都會跳出配點畫面（正式為每 5 波一次），方便快速測試配點面板 | `killsPerWave` 用 `CONFIG.survival.killsPerWave`（8），配點只在 `completedWave % CONFIG.survival.waveInterval === 0` 時觸發 |

**訪談結論：三個旗標暫時保留現狀，不需要立即處理。** 之後要在某個里程碑（例如：內容擴充到一定程度、要正式對外分享之前）再一併檢視並改回正常行為。這裡先記錄清楚，避免未來忘記這是「測試殘留」而非刻意設計。

## 4. Non-Goals / Constraints

- **單檔 / `file://` 雙擊約束：已解除。** 原本訪談時確認為硬需求，後續討論部署管道與拆檔案的實際壞處（可維護性、版控/協作、快取、工具鏈四個面向，詳見 §7 開放問題第 3 項的決策記錄）後，**明確決定解除此約束**。
- **決定後的新方向**：
  - 主要部署管道改為 **GitHub Pages**（或同等的靜態 HTTPS 託管）。在真實 HTTPS 同源環境下，`<script type="module">` 的 `import` 之間完全不受 CORS 限制，可以放心把遊戲本體拆成多個標準 ES module 檔案（例如 `tank.js`、`ai.js`、`config.js`），不需要再遷就 `file://` 的限制。
  - 本地開發測試**可以接受起一個簡單的本地伺服器**（例如 `python3 -m http.server` 或編輯器內建的 Live Server），不再要求雙擊 `.html` 就能直接運作。
  - 原本記錄的「傳統 `<script src>` 拆檔案」變通方案（見歷史討論）已不再需要，直接採用標準 `import`/`export` 即可。
- **後續行動**：實際的拆檔案工作尚未開始，屬於 §5.2「程式碼健康」方向下的具體任務，拆分方式與檔案結構待規劃（不在本次決策範圍內）。

## 5. Next-Iteration Requirements

訪談中你選擇的下一步優先方向是「更多內容、程式碼健康、打磨體驗」三者並重，**沒有給出明確排序**，因此以下三大方向都列為有效的下一步選項，不虛構優先順序：

### 5.1 更多內容（Content）
- **✅ 無限生存模式（波次制 + 技能點 + 方位裝甲 + 武器/飛彈升級）已實作**，詳見 §6.2。
- 剩餘候選方向（皆待進一步規劃，非承諾項目）：新敵人種類（例如不同 AI 行為、不同武器/血量，闖關模式需要）、闖關模式本身（§6.3）。

### 5.2 程式碼健康（Code Health）
- **✅ 拆成多個 ES module 檔案已完成**（見 §2.1 檔案結構）：`src/game.js`（主邏輯+進入點）、`src/tank.js`（坦克模型）、`src/audio.js`（音效/音樂）、`src/marseillaise-midi.js`（MIDI 資料）、`src/menu.js`（模式選單）、`src/survival.js`（無限生存邏輯）。拆分時做過逐行 diff 驗證，確認邏輯無遺漏。
- **✅ CONFIG 抽取已完成**：新增 `src/config.js`，把所有「玩法可調參數」（速度/傷害/冷卻/計時器/鏡頭距離/生成間隔/波次與技能點公式等，共約 60 個欄位）集中成一個 `CONFIG` 物件，`game.js` 與 `survival.js` 改成讀取 `CONFIG.xxx.yyy`。**刻意不收**的：坦克/飛彈的幾何造型尺寸（`tank.js`、`makeMissileMesh`）、顏色、`audio.js` 的合成器音色參數與背景音樂——這些是外觀/音色而非玩法旋鈕，維持留在原地。抽取時逐一比對過所有數值跟原本硬編碼的字面值完全一致，行為不變。
- 日後隨 §6 新模式（闖關）開發，`game.js` 可能還會再進一步拆分（例如 `ai.js`、`powerups.js`），屬於自然演進，不在本次範圍內。

### 5.3 打磨體驗（Polish）
- 候選方向：UI/UX 改善、平衡調整。**訪談時你表示目前沒有已知的手感/平衡/bug 抱怨**（見 §7），所以這個方向目前沒有具體待辦項目，屬於「有餘力再做」的狀態，而非有明確痛點驅動。

## 6. 玩法方向提案：新增遊戲模式（無限生存已實作，闖關待實作）

> 本節記錄新遊戲模式的設計與實作狀態。無限生存模式（§6.2）已完整實作；闖關模式（§6.3）仍只是設計提案，程式碼尚未開始。

### 6.1 主選單與模式切換 —— ✅ 已實作

- `src/menu.js`：開場顯示模式選單（`#menu`），四個按鈕：
  1. **無限亂鬥**（現有玩法，敵人持續刷新，無波次/關卡概念）—— 可玩
  2. **無限生存**（見 §6.2）—— 可玩
  3. **闖關模式**（見 §6.3）—— 按鈕存在但標示「開發中」並停用（`disabled`），尚未實作
  4. **測試模式**（見 §6.5）—— 可玩，QA/除錯用，可自由選敵人種類、關卡、直接套用裝甲/武器/飛彈數值
- 選定模式前，場景照常渲染（背景可看到地圖與待命的玩家坦克），但玩法邏輯（`game.js` 的 `gameStarted` 旗標）不會啟動，避免出現「按鈕看得到但按了沒反應」的半成品體驗。

### 6.2 模式：無限生存（Infinite Survival）—— ✅ 已實作

- **波次系統**：擊殺數制，每波固定擊殺 **8 隻**敵人才算過關（沿用現有同時最多 3 隻存在的上限，持續補到殺滿 8 隻），接著生成下一波。敵人強度隨波次遞增（數量不變，靠強度撐難度）：
  - `hp = 30 × (1 + 0.12 × (波次 - 1))`
  - `傷害 = 12 × (1 + 0.08 × (波次 - 1))`
  - 範例：第 10 波 hp≈62、傷害≈21。
- **獎勵節奏**：每存活 5 波 → 獲得 10 技能點，**無上限，可以全押同一類**。
- **技能點分配 UI（新畫面）**：每波過關後**完全暫停遊戲**，跳出配點畫面（視覺風格比照現有 `#overlay`），列出 6 個類別（前/後/左/右裝甲、主武器火力、特殊武器火力），各自 +/- 按鈕即時調整、顯示剩餘點數，確認後才恢復遊戲進下一波。
- **方位裝甲**（前 / 後 / 左 / 右 分別疊加）：
  - 命中判定：子彈命中時計算「命中角度相對於車身朝向」，落在哪個 90 度象限就套用對應方位的減傷。現有碰撞邏輯只算距離、不算角度，這是全新邏輯，需要新增「命中方位判定」函式。
  - 減傷公式：`減傷 = min(0.8, 點數 × 0.05)`（每點 5%，上限 80%，16 點封頂）。
  - 死亡機制不特殊化：純粹減傷，HP 歸零就死，跟現有邏輯一致。
- **主武器火力**：
  - `冷卻 = max(0.1, 0.35 × 0.97^點數)`
  - `傷害 = 10 × 1.08^點數`
- **特殊武器火力（飛彈：從隨機道具改造成主動技能）**：
  - 觸發方式從「撿道具觸發」改成固定冷卻的主動技能，按 **Q 鍵**觸發（`missileAbilityCooldown` 計時，HUD 顯示冷卻中/就緒）。
  - 加點強化兩個面向（不強化傷害，因為飛彈鎖定就必殺，加傷害沒意義）：
    - 冷卻時間：`冷卻 = max(10, 30 × 0.95^點數)`（基礎 30 秒）
    - 飛行速度：`速度 = 22 × 1.08^點數`（基礎速度沿用現有 `speed: 22`）。追蹤靈敏度目前**未**隨點數提升（維持原本固定轉向速率），僅飛行速度隨點數變快。

**實作位置**：`src/menu.js`（模式選單）、`src/survival.js`（波次/技能點/裝甲/武器/飛彈數值 + 配點 UI 邏輯）、`src/game.js`（串接：`gameMode`/`gameStarted` 旗標、`spawnEnemy()` 套用波次強度、`shoot()` 套用武器加成、子彈命中玩家時套用方位裝甲減傷、`onEnemyKilled()` 處理波次清算與配點畫面觸發、`Q` 鍵觸發飛彈技能）。HUD 新增目前波次/擊殺進度與飛彈冷卻顯示。

### 6.3 模式：闖關模式（Campaign）—— 底層素材已備妥（敵人種類/關卡資料/地形效果），過關流程/進度/道具解鎖尚未串成可玩模式

- **確認決定的三個變化維度**：
  1. **地圖/掩體佈局每關不同**：**✅ 已完成** `src/levels.js`，10 關手工設計的關卡資料（`LEVELS`），每關 `{ name, terrain, walls, dust? }`。牆體旋轉（見 §2.2）讓佈局能用真正的斜角排列（環陣、三角陣、菱堡等）。沙漠地形另有 `dust` 沙塵區（見下方「地形效果」）。
  2. **敵人種類/數量隨關卡遞增**：新敵人類型（偵查兵/重裝兵）**✅ 已實作**，見下方。「隨關卡遞增配置」這件事本身（哪關出現哪些種類/數量）仍待闖關模式的過關流程串起來才有意義，目前僅能在測試模式（§6.5）手動勾選。
  3. **道具池隨進度解鎖**：現有三種道具（`shield` / `rapid` / `missile`）一開始就全部存在，改成隨破關進度**逐步解鎖**——**尚未實作**，需要闖關模式的進度系統才有意義。

- **關卡總數**：MVP 先手工設計 **10 關**，資料已在 `src/levels.js`（4 種地形家族：開闊地帶 ×4、城市廢墟 ×2、沙漠 ×2、叢林 ×2）。之後要不要接續無限規則生成尚未決定，先記錄為之後再議的方向。

- **地形效果（已隨關卡資料一併實作）**：沙漠關卡（第 7、8 關）有沙塵區（`dust: [{x, z, r}]`），站在區域內移動速度打 `CONFIG.terrain.dustSlowMult`（0.5）折，玩家與敵人皆適用（`dustSpeedMult()`，見 `src/game.js`）。

- **過關條件**：**混合制**——同時實作「清光所有敵人」與「存活到時間到（計時制）」兩套邏輯，10 關裡具體哪關用哪種，等實際串接過關流程時再逐關決定——**尚未實作**（目前 `levels.js` 只有地圖資料，沒有過關判定/進度串接）。

- **新敵人類型（✅ 已實作，`CONFIG.enemyTypes`，見 `src/config.js`）**：

  | 數值 | 標準兵（現有，不變） | 偵查兵 Scout（新） | 重裝兵 Heavy（新） |
  |---|---|---|---|
  | HP | 30 | **15**（脆） | **60**（肉盾） |
  | 速度 | 4.5 | **7.5**（快） | **2.5**（慢） |
  | 傷害 | 12 | **8**（弱但煩） | **20**（重拳） |
  | 轉向速度 | 正常 | 正常 | **×0.7**（遲鈍） |

  - **AI 行為差異化**（跟標準兵共用的「逼近／繞圈／拉開」三狀態機比，各拿掉一種狀態，靠 `canEngage`/`canRetreat` 旗標控制）：
    - **偵查兵**：拿掉「逼近」狀態，只在「繞圈」與「拉開」間切換——不貼身肉搏，保持距離游擊。
    - **重裝兵**：拿掉「拉開」狀態，只在「逼近」與「繞圈」間切換——不主動撤退，笨重但不怕死。
  - **視覺辨識**：沿用現有 `makeTank()` 模型（不重新設計造型），用顏色 + 縮放區分（偵查兵縮小顯瘦、重裝兵放大顯壯）。

- **目前未串成可玩模式的部分**：過關判定、關卡進度（第幾關→下一關）、道具解鎖節奏、`gameMode === 'campaign'` 的實際遊戲流程——`src/menu.js` 的「闖關模式」按鈕仍是 `disabled`。已備妥的敵人種類與關卡資料目前透過測試模式（§6.5）曝露出來供除錯/試玩用。

### 6.4 與現有優先方向的關係

- 這兩個模式屬於 §5.1「更多內容」方向下的具體提案，工作量評估**明顯大於**當初訪談時列出的候選項目——這次討論等於是把「新敵人種類、更多道具、關卡/波次制、難度遞增」這些候選項目具體化，並直接綁定成兩個完整模式。
- 無限生存（§6.2）已實作完成；闖關模式（§6.3）的敵人種類與關卡資料已實作，過關流程/進度/道具解鎖仍待串接才能成為可玩模式。

### 6.5 模式：測試模式（Test Mode）—— ✅ 已實作

> 為了在闖關模式的過關流程尚未完成之前，就能對新敵人種類、關卡佈局、裝甲/武器數值做手動驗證，新增的 QA/除錯用沙盒模式。不算是給一般玩家玩的「正式模式」。

- **入口**：主選單第 4 個按鈕「測試模式」（`#modeTest`），點擊後不直接開始遊戲，而是彈出設定面板（`#testMenu`，`src/testmode.js`）。
- **可設定項目**：
  1. **敵人種類**（可複選，checkbox）：標準兵/偵查兵/重裝兵，至少要選一種（沒選視同只選標準兵）。遊戲中每次生成敵人會從勾選的種類池隨機挑一種（`testEnemyTypePool`）。
  2. **關卡**（下拉選單，`#testLevelSelect`）：選項由 `LEVELS`（`src/levels.js`）動態產生，選定後套用該關的牆體佈局（`loadLevelWalls()`，會保留外圍邊界牆、只替換內部掩體）與沙塵區（`dustZones`）。
  3. **玩家技能點數值**（6 個數字輸入框）：前/後/左/右裝甲、主武器火力、特殊武器火力，直接寫入 `SURVIVAL.upgrades`（不用像無限生存那樣打波次累積點數）。
- **實作方式**：重用無限生存模式（§6.2）的技能點數值公式與飛彈技能（Q 鍵）——新增 `usesUpgrades()` 判斷式（`gameMode === 'survival' || gameMode === 'test'`），凡是「套用裝甲減傷/武器冷卻傷害/飛彈技能」的地方都改用這個判斷式，但波次相關邏輯（`onEnemyKilled()` 的波次清算、配點畫面彈出、HUD 波次顯示、`gameOver()` 波次字樣）維持**只有 `survival` 才會觸發**，測試模式沒有波次概念，選好設定就直接進入自由測試，沒有勝敗條件。
- **不做的事**：測試模式不記錄成績、沒有過關/波次進度，純粹是設定完就丟進場景讓你手動驗證數值與行為，關掉分頁/重整即結束。

## 7. Open Questions / Decisions Log

1. ~~CONFIG 抽取是否要做？~~ —— **✅ 已決定並完成。** 新增 `src/config.js`，詳見 §5.2。

2. **兩個測試旗標的回復時機點** —— 訪談決定「先保留，之後再改」，沒有指定具體里程碑。
   - 我的建議：建議至少在「要讓別人（不只自己）試玩」之前回復成正常行為，否則背景音樂永遠只有一首、道具永遠都是飛彈，會讓體驗失真。可以把「回復兩個測試旗標」列為對外分享前的檢查清單項目之一。

3. ~~單檔約束本身是否要保留？~~ —— **✅ 已決定（解除約束），且已實際完成拆檔案。** 原本訪談確認為硬需求，後來衡量拆檔案的實際壞處（可維護性、版控/協作、快取、工具鏈）與 GitHub Pages 部署不受此限制等因素後，決定解除約束並直接動工：`index.html` 現在只是外殼，邏輯拆進 `src/game.js` / `src/tank.js` / `src/audio.js` / `src/marseillaise-midi.js`（結構見 §2.1），本地測試改用 `python3 -m http.server` 之類的本地伺服器。這一項不再是開放問題，保留在此作為決策記錄。

4. **`music.html` 的定位** —— 目前是開發過程的原型/沙盒頁面，未被納入正式功能範圍，也沒有討論是否要保留、刪除或整理成文件。
   - 我的建議：如果不打算再用它做其他音樂原型實驗，可以考慮保留當作「如何用 base64 內嵌 MIDI + Tone.js 播放」的參考範例，或視情況清理；不影響遊戲本體運作，優先權低。

5. **目前已知痛點** —— 訪談時明確表示「沒有，目前都還好」，未發現手感/平衡/bug 相關抱怨。此為撰寫本文件當下的狀態記錄，之後如果發現問題，應回來更新本節。

6. ~~無限生存模式的數值細節~~ —— **✅ 已決定，完整數值方案見 §6.2。** 波次靠敵人強度遞增（每波固定殺 8 隻）、技能點無上限、方位裝甲每點 5% 減傷上限 80%、死亡機制不特殊化、飛彈從隨機道具改成主動技能（加點強化冷卻與飛行速度）、新增配點 UI（過關後完全暫停）。尚未開始實作，屬於下一步的具體任務。

7. ~~闖關模式的結構細節~~ —— **✅ 大部分已決定，完整內容見 §6.3。** 關卡總數 MVP 先手工做 10 關（之後要不要接無限生成尚未決定）、過關條件混合制（清怪/計時，逐關決定用哪種）、新敵人類型定案為偵查兵 + 重裝兵兩種（數值、AI 差異化、視覺辨識皆已定案）。**尚未決定的**：10 關實際的地圖佈局/敵人配置/道具解鎖節奏/過關條件分配，這些屬於「排關卡內容」這個下一步任務，尚未開始。
